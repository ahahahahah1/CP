#include <stdio.h>

int main () {
    int t, n, i, last_index, resolved, size;

    scanf("%d", &t);
    
    while(t--) {
        last_index = 0;
        resolved = 0;
        scanf("%d", &n);

        int b[n];
        for(i=0; i<n; i++) {
            scanf("%d", b+i);
        }

        if(check(b, n, 0, )
    }

    return 0;
}